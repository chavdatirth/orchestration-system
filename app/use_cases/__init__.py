"""Use Cases Layer.

Contains application-specific business logic. Coordinates domain entities
and defines abstract ports (interfaces) for communicating with external adapters
(such as LLM providers or databases).
"""
